[pytest]
# Collect only our own suite. The vendored LVGL checkout carries its own test
# scripts, one of which calls sys.exit() at import time and takes the whole run
# with it -- third-party sources are dependencies, not tests.
testpaths = tests
norecursedirs = third_party .venv build sim-out .git
