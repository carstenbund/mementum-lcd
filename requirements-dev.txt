# Phase 0c harness. The core and the simulator are stdlib-only by design:
# nothing here is imported by mementum_node/ or sim/ at runtime.
pytest==9.1.1

# The screen. `drm_screen_lvgl` is the LVGL renderer plugin -- it binds the C in
# poc/player/, so this repository is where its library is built. `drm_screen`
# brings the service and the numpy compositor and is optional: a node without it
# runs mementum_node.screen.service.SimpleScreenService instead.
drm-screen-lvgl>=0.1.1
drm-screen ; python_version >= "3.10"

# `drm_composer` compiles screen-HTML to a command batch, and for a layer of
# <path> elements that batch is a drm_scene_ir document -- which is what the C
# player loads.  tests/test_composer_to_device.py checks exactly that, and
# importorskip's past it when the package is absent, so without this line the
# suite reports a pass having never run it.  <path>/<animate> arrived in 0.2.
drm-composer>=0.2

# The control server (mementum_node/server/): flask and requests, matching
# mementum-led's raspberry/requirements.txt so one Pi can run both.
flask>=3.0
requests>=2.28
